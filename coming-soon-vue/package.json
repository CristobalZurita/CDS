{
  "name": "cds-coming-soon",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp public/mail.php dist/ && cp public/.htaccess dist/",
    "package": "npm run build && rm -f cds-coming-soon.zip && cd dist && zip -rq ../cds-coming-soon.zip .",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.4.1"
  }
}
